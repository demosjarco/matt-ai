import { component$ } from '@builder.io/qwik';

export default component$(() => {
	return <div class="flex h-10 w-10 flex-shrink-0 items-center justify-center rounded-full bg-indigo-500">A</div>;
});
